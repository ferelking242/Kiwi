<?xml version="1.0" encoding="utf-8"?>
  <!--
    Eterna Browser — AndroidManifest overlay
    Package: com.aivos.eterna
    This file is merged by the build system over the base Chrome manifest.
  -->
  <manifest xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      package="com.aivos.eterna">

      <!-- Foreground service for Immortal Mode -->
      <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
      <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
      <uses-permission android:name="android.permission.WAKE_LOCK" />
      <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

      <!-- Shizuku (optional) -->
      <queries>
          <package android:name="moe.shizuku.privileged.api" />
      </queries>

      <application
          android:label="@string/app_name"
          android:icon="@mipmap/ic_launcher"
          android:roundIcon="@mipmap/ic_launcher_round"
          tools:replace="android:label,android:icon,android:roundIcon">

          <!-- Immortal Mode foreground service -->
          <service
              android:name="com.aivos.eterna.immortal.ImmortalModeService"
              android:exported="false"
              android:foregroundServiceType="dataSync" />

          <!-- Session recovery on boot -->
          <receiver
              android:name="com.aivos.eterna.session.BootReceiver"
              android:exported="true">
              <intent-filter android:priority="1000">
                  <action android:name="android.intent.action.BOOT_COMPLETED" />
                  <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                  <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
              </intent-filter>
          </receiver>

          <!-- Download recovery on boot -->
          <receiver
              android:name="com.aivos.eterna.download.DownloadBootReceiver"
              android:exported="true">
              <intent-filter>
                  <action android:name="android.intent.action.BOOT_COMPLETED" />
              </intent-filter>
          </receiver>
      </application>
  </manifest>
  